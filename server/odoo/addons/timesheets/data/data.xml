<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Sequences for helpdesk_lite.ticket -->
        <record id="seq_timesheet" model="ir.sequence">
            <field name="name">Timesheet Entry</field>
            <field name="code">timesheet.entry</field>
            <field name="prefix">TS</field>
            <field name="padding">4</field>
        </record>

        <!-- Sequences for sales.timesheet -->
        <record id="sales_seq_timesheet" model="ir.sequence">
            <field name="name">Sales Timesheet Entry</field>
            <field name="code">sales.timesheet.entry</field>
            <field name="prefix">ST</field>
            <field name="padding">5</field>
        </record>

        <!-- Sequences for tech.timesheet -->
        <record id="tech_seq_timesheet" model="ir.sequence">
            <field name="name">Tech Timesheet Entry</field>
            <field name="code">tech.timesheet.entry</field>
            <field name="prefix">TE</field>
            <field name="padding">5</field>
        </record>

        <!-- Sequences for hr.timesheet -->
        <record id="hr_seq_timesheet" model="ir.sequence">
            <field name="name">HR Timesheet Entry</field>
            <field name="code">hr.timesheet.entry</field>
            <field name="prefix">HE</field>
            <field name="padding">5</field>
        </record>

        <!-- Sequences for marketing.timesheet -->
        <record id="marketing_seq_timesheet" model="ir.sequence">
            <field name="name">Marketing Timesheet Entry</field>
            <field name="code">marketing.timesheet.entry</field>
            <field name="prefix">MT</field>
            <field name="padding">5</field>
        </record>

          <!-- Sequences for stores.timesheet -->
        <record id="marketing_seq_timesheet" model="ir.sequence">
            <field name="name">Stores Timesheet Entry</field>
            <field name="code">stores.timesheet.entry</field>
            <field name="prefix">ST</field>
            <field name="padding">5</field>
        </record>

         <!-- Sequences for account.timesheet -->
        <record id="account_seq_timesheet" model="ir.sequence">
            <field name="name">Account Timesheet Entry</field>
            <field name="code">account.timesheet.entry</field>
            <field name="prefix">AT</field>
            <field name="padding">5</field>
        </record>

        <!-- Sequences for csc.timesheet -->
        <record id="csc_seq_timesheet" model="ir.sequence">
            <field name="name">CSC Timesheet Entry</field>
            <field name="code">csc.timesheet.entry</field>
            <field name="prefix">CS</field>
            <field name="padding">5</field>
        </record>

    </data>
</odoo>